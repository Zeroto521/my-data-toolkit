name: dtoolkit
channels:
  - conda-forge
dependencies:
  # required
  - pandas >= 2.0.3
  # optional
  - scikit-learn >= 1.7
  - packaging
  - geopandas >= 1.1.0
  - geopy
  - jenkspy
  - topojson
  - h3-py >= 4.1.2
  - joblib
  - rapidfuzz

  - pip
  - pip:
      - my-data-toolkit
