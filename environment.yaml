name: dtoolkit
channels:
  - conda-forge
dependencies:
  # required
  - pandas >= 1.5.1
  # optional
  - scikit-learn >= 1.1
  - packaging
  - geopandas >= 0.12.1
  - geopy
  - jenkspy
  - topojson
  - joblib
  - rapidfuzz

  - pip
  - pip:
      - my-data-toolkit
