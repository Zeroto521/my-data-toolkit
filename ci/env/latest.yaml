name: test
channels:
  - conda-forge
dependencies:
  # required
  - pandas
  # optional
  - scikit-learn
  - packaging
  - geopandas
  - geopy
  - jenkspy
  - joblib
  - rapidfuzz
  - h3-py
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  # doctest testing
  - pytest-doctestplus

  - pip
  - pip:
    - zhconv
