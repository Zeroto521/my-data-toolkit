name: test
channels:
  - conda-forge
dependencies:
  # required
  - pandas
  # optional
  - scikit-learn
  - geopandas
  - pygeos
  - geopy
  - jenkspy
  - topojson
  - joblib
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  # doctest testing
  - pytest-doctestplus

  - pip
  - pip:
      # optional
      - simplification
