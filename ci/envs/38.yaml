name: test
channels:
  - conda-forge
dependencies:
  - python=3.8
  # required
  - pandas
  - geopandas
  - scikit-learn
  - more-itertools
  - pygeos
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  # doctest testing
  - pytest-doctestplus
