name: test
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  # required
  - pandas
  - geopandas=0.9.*
  - scikit-learn
  - more-itertools
  - packaging
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  # doctest testing
  - pytest-doctestplus
