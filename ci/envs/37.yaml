name: test
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  # required
  - pandas
  - geopandas
  - scikit-learn
  # testing
  - pytest
  - codecov
  - pip:
      # testing
      - coverage
