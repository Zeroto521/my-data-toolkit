name: test
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  # required
  - pandas
  - geopandas
  - scikit-learn
  - more-itertools
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
