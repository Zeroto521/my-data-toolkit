name: test
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  # required
  - pandas
  - geopandas=0.10.*
  - scikit-learn
  - more-itertools
  # testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  # doctest testing
  - pytest-doctestplus
